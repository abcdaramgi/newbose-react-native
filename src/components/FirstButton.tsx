import React, {FC} from 'react';
import {Text, View} from 'react-native';

const FirstButton: FC = () => {
  return (
    <View>
      <Text>FirstButton</Text>
    </View>
  );
};

export default FirstButton;
